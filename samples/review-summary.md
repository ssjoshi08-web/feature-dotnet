# AI Review Summary

**Pipeline stage:** AI Review Agent
**Provider:** `claude`
**Status:** ❌ **FAIL**

## Findings Overview

| Category             | Count |
|----------------------|-------|
| Security findings    | **2** |
| Code smells          | 3     |
| Architecture issues  | **1** |
| Test coverage issues | 1     |

## Summary

> Critical security issue detected: a hardcoded AWS access key was found in
> `src/services/StorageService.cs`. Additionally, a missing authorization
> check on `PUT /api/users/{id}` violates OWASP A01. Code-quality and
> architecture findings are advisory.

## Coverage

| Metric           | Value  | Threshold | Status |
|------------------|--------|-----------|--------|
| Line coverage    | 76.4 % | 80 %      | ❌     |
| Branch coverage  | 68.1 % | 80 %      | ❌     |

## Recommendations

1. Remove the hardcoded AWS access key from `src/services/StorageService.cs`
   and load it from `AWS_ACCESS_KEY_ID` environment variable or AWS Secrets
   Manager.
2. Add an authorization attribute
   (`[Authorize(Roles="Admin")]`) to `UserController.Update`.
3. Refactor `OrderProcessor.CalculateTotals` (cyclomatic complexity 14)
   into smaller helpers.
4. Add unit tests for `PaymentService.Refund` covering the partial-refund
   edge case.
5. Move `NotificationService.SendEmail` out of the Domain layer; it imports
   MailKit, violating layering rules.

## Detailed Findings

### 🔴 Security

| ID      | Severity | File                                    | Line | Description                                                  |
|---------|----------|-----------------------------------------|------|--------------------------------------------------------------|
| SEC-001 | CRITICAL | `src/services/StorageService.cs`        | 27   | Hardcoded AWS access key (AKIA…). Rotate immediately.        |
| SEC-002 | HIGH     | `src/api/Controllers/UserController.cs` | 88   | Missing authorization on `PUT /api/users/{id}` (OWASP A01). |

### 🟡 Code Quality

| ID    | Type           | File                                          | Line | Description                                          |
|-------|----------------|-----------------------------------------------|------|------------------------------------------------------|
| CS-001| complexity     | `src/domain/OrderProcessor.cs`                | 134  | `CalculateTotals` cyclomatic complexity = 14.        |
| CS-002| duplicate_code | `src/api/Controllers/OrderController.cs`      | 45   | Validation duplicated from `CartController.AddItem`.|
| CS-003| naming         | `src/utils/Helpers.cs`                        | 12   | Variable `d` for discount amount — non-descriptive.  |

### 🔴 Architecture

| ID      | Severity | From            | To                              | Description                                          |
|---------|----------|-----------------|---------------------------------|------------------------------------------------------|
| ARCH-001| HIGH     | `src/domain`    | `src/infrastructure/MailKit`    | `NotificationService` imports MailKit (layer leak).  |

### 🟡 Testing

| ID     | Type           | File                              | Description                                                  |
|--------|----------------|-----------------------------------|--------------------------------------------------------------|
| TST-001| missing_tests  | `tests/PaymentServiceTests.cs`    | No tests for `PaymentService.Refund` partial-refund case.    |

## Decision

**Pipeline blocked.** The review failed because:

* A CRITICAL hardcoded secret was detected.
* Coverage fell below the configured 80 % threshold.
* A HIGH-severity architecture layer violation was detected.

---
*Generated by the Provider-Agnostic AI Review Agent.*
*Artefacts: `review-report.json`, `review-summary.md`.*